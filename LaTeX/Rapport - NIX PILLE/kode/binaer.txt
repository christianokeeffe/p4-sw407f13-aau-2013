private List<ResultatListenMedAntalChecker> FletMedEkstisterendeListHvisNoegleordFindes(ListElementInfo ElementDerSkalTilfoejes, List<ResultatListenMedAntalChecker> Primaere, ListElementInfo InputA, ListElementInfo InputB, int Start, int Slut)
{
  //Indsæt elementet efter princippet bag binær søgning
  if (Primaere.Count != 0 && Slut-Start >= 0)
  {
    //Find midten af søgeområdet
    int midt = ((Slut - Start) / 2) + Start;

    //Sammenlign elementet med midten
    int Sammenligning = Primaere[midt].Overskrift.CompareTo(ElementDerSkalTilfoejes.Overskrift);

    if (Sammenligning == 1 && midt-Start != 0)
    {
      //Kald funktionen igen, kørende på den første halvdel af området i listen
      Primaere = FletMedEkstisterendeListHvisNoegleordFindes(ElementDerSkalTilfoejes, Primaere, InputA, InputB, Start, midt - 1);
    }
    else if (Sammenligning == -1 && Slut-midt != 0)
    {
      //Kald funktionen igen, kørende på den sidste halvdel af området i listen
      Primaere = FletMedEkstisterendeListHvisNoegleordFindes(ElementDerSkalTilfoejes, Primaere, InputA, InputB, midt + 1, Slut);
    }
    else if (Sammenligning == 0)
    {
      //Elementet findes allerede, og antallet tilføjes
      Primaere[midt] = Kombiner2Noegleord(Primaere[midt], InputA, InputB);
    }
    else
    {
      //Elementet findes ikke, og det tilføjes derfor til den korrekte position
