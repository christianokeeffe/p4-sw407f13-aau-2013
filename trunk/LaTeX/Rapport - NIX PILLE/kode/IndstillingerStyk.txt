// checker om ny adgangskode og gentag adgangskode er ens
if (txtNyAdgangskode.Text == txtGentagAdgangskode.Text)
{
  // checker om det gamle krypteret kodeord er tom, for at sikre mod fejl
  if (GammeltKrypteretKodeord != "")
  {
    // checker om den nye adgangskode er større end 6 tegn og mindre end 60 tegn 
    if (8 <= txtNyAdgangskode.Text.Length && txtNyAdgangskode.Text.Length <= 60)
    {
      // checker om den gamle adgangskode passer
      if (BCrypt.Net.BCrypt.Verify(txtGamleAdgangskode.Text, GammeltKrypteretKodeord))
      {
        Global.DatabaseKaldEksekvering("UPDATE brugere SET Kodeord='" + BCrypt.Net.BCrypt.HashPassword(txtNyAdgangskode.Text) + "' WHERE ID = '" + NuvaerendeBruger.BrugerID.ToString() + "'", ShoplistDatabase);
        lblKodefejl.Text = "Koden er nu ændret";
