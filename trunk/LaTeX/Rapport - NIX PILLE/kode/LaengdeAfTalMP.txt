//Da komma ikke godkendes som specialtegn med IsNumber tages det med
if (Char.IsNumber(BeskrivelseAfVare[StartPunkt + LængdeTilTalStart + LængdeAfTal]) ||
  BeskrivelseAfVare[StartPunkt + LængdeTilTalStart + LængdeAfTal] == ',')
{
  if (BeskrivelseAfVare[StartPunkt + LængdeTilTalStart + LængdeAfTal] == ',' &&
    StartPunkt + LængdeTilTalStart + LængdeAfTal + 1 < BeskrivelseAfVare.Length)
  {
    if (!char.IsNumber(BeskrivelseAfVare[StartPunkt + LængdeTilTalStart + LængdeAfTal + 1]))
    {
      LængdeAfTal--;
      StadigTal = false;
    }
  }
  LængdeAfTal++;
